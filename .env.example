# Answer is 42 - Variables d'environnement
# Copiez ce fichier en .env et remplissez les valeurs

# Configuration serveur
NODE_ENV=production
PORT=5000

# Base de données PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/answeris42

# Configuration SMTP pour l'envoi d'emails
# Fournisseurs recommandés: Brevo (300/jour gratuit), Mailjet, Gmail SMTP, OVH Mail
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=votre-email@domaine.fr
SMTP_PASS=votre-mot-de-passe
SMTP_FROM=contact@is42.fr

# Email de destination des messages du formulaire de contact
CONTACT_EMAIL=contact@is42.fr
